
# 🎓 Academia Albert - Moçambique

Este é o site institucional completo da **Academia Albert**, focado em educação profissional em Moçambique com sistema de pagamentos integrado.

## 📱 Transformar em Aplicativo Móvel

### Opção 1: PWA (Mais fácil)
1. Abra o site no Chrome (Android) ou Safari (iOS).
2. Clique em "Adicionar à tela inicial" ou "Instalar Aplicativo".
3. Pronto! O site agora se comporta como um app.

### Opção 2: Nativo (Google Play / App Store)
Para gerar o arquivo `.apk` ou `.ipa`:
1. Instale as dependências: `npm install`.
2. Gere o build do site: `npm run build`.
3. Inicialize o Capacitor: `npx cap init`.
4. Adicione as plataformas: 
   - `npx cap add android`
   - `npx cap add ios`
5. Sincronize o código: `npx cap sync`.
6. Abra no Android Studio ou Xcode: `npx cap open android`.

## 🚀 Como Rodar Localmente
1. `npm install`
2. `npm run dev`

---
*Desenvolvido com excelência para a educação em Moçambique.*
